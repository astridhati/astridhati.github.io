---
import Layout from "../layouts/Layout.astro";
import Hero from "../components/Hero.astro";
import { getSite } from "../lib/content";
import { homeTitle, siteBio, siteTagline, siteWelcome } from "../lib/i18n";
import type { Locale } from "../lib/types";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const site = getSite();
const welcome = siteWelcome(site, locale);

const title = homeTitle(site, locale);
const description = siteBio(site, locale).slice(0, 160);
const siteUrl = "https://astridhati.github.io";
const canonical = locale === "en" ? `${siteUrl}/en/` : `${siteUrl}/`;

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Person",
  name: site.name,
  description: siteBio(site, locale),
  url: canonical,
  jobTitle: siteTagline(site, locale),
  sameAs: Object.values(site.social ?? {}).filter(Boolean),
};
---

<Layout
  locale={locale}
  title={title}
  description={description}
  image={site.logo}
  canonical={canonical}
  jsonLd={jsonLd}
>
  <Hero site={site} locale={locale} />

  {
    welcome && (
      <section class="home-welcome section">
        <div class="container">
          <p class="home-welcome__text">{welcome}</p>
        </div>
      </section>
    )
  }
</Layout>

<style lang="scss">
  .home-welcome {
    text-align: center;
  }

  .home-welcome__text {
    max-width: 36rem;
    margin: 0 auto;
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--color-text-muted);
  }
</style>
