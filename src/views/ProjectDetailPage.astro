---
import Layout from "../layouts/Layout.astro";
import GalleryGrid from "../components/GalleryGrid.astro";
import { getDrawingsByProject, getSite } from "../lib/content";
import { getUi, localizedPath } from "../lib/i18n";
import type { Locale, Project } from "../lib/types";
import {
  assetPath,
  projectDescription,
  projectName,
  projectSeoDescription,
  projectSeoTitle,
} from "../lib/types";

interface Props {
  locale: Locale;
  project: Project;
}

const { locale, project } = Astro.props;
const site = getSite();
const drawings = getDrawingsByProject(project.id);
const strings = getUi(locale);
const siteUrl = "https://astridhati.github.io";
const canonical = `${siteUrl}${localizedPath(`/progetti/${project.id}/`, locale)}`;

const title = projectSeoTitle(project, site.name, locale);
const description = projectSeoDescription(project, site.name, locale);

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  name: projectName(project, locale),
  description: projectDescription(project, locale),
  url: canonical,
  author: {
    "@type": "Person",
    name: site.name,
  },
};
---

<Layout
  locale={locale}
  title={title}
  description={description}
  image={project.cover}
  canonical={canonical}
  jsonLd={jsonLd}
>
  <section class="projects section" aria-labelledby="project-title">
    <div class="container stack stack--lg stack--pad-top">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <img
          src="/images/star.png"
          alt=""
          class="breadcrumb__star"
          width="18"
          height="18"
          aria-hidden="true"
        />
        <a href={localizedPath("/progetti/", locale)}>{strings.projects.breadcrumb}</a>
        <span class="breadcrumb__sep" aria-hidden="true"> / </span>
        <span class="breadcrumb__current" aria-current="page">{projectName(project, locale)}</span>
      </nav>

      <header class="project-header">
        <div class="project-header-image">
          <img
            src={assetPath(project.cover)}
            alt={projectName(project, locale)}
            width="800"
            height="500"
            loading="eager"
          />
        </div>
        <div class="project-header-body stack stack--sm">
          <h1 id="project-title" class="section-title">{projectName(project, locale)}</h1>
          {project.year && <p class="project-card-year">{project.year}</p>}
          {projectDescription(project, locale) && (
            <p class="project-header-description">{projectDescription(project, locale)}</p>
          )}
        </div>
      </header>

      <h2 class="section-title section-title--sub">{strings.projects.works}</h2>
      <GalleryGrid
        drawings={drawings}
        locale={locale}
        emptyMessage={strings.gallery.emptyProject}
      />
    </div>
  </section>
</Layout>

<style lang="scss">
  @use "../styles/breakpoints" as *;

  .breadcrumb {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.375rem;
    font-family: var(--font-display);
    font-size: 1rem;
    letter-spacing: 0.02em;
    color: var(--color-text-muted);

    &__star {
      flex-shrink: 0;
      width: 1.125rem;
      height: 1.125rem;
    }

    a {
      color: var(--color-teal);
      text-decoration: none;

      &:hover {
        color: var(--color-pink);
      }
    }

    &__sep {
      opacity: 0.5;
      user-select: none;
    }

    &__current {
      color: var(--color-pink);
    }
  }

  .project-header {
    display: grid;
    gap: var(--space-md);

    @include from-md {
      grid-template-columns: 1fr 1fr;
      align-items: center;
    }
  }

  .project-header-image img {
    width: 100%;
    height: auto;
    border-radius: var(--radius);
    object-fit: cover;
  }

  .project-header-description {
    line-height: 1.6;
  }

  .section-title--sub {
    font-size: 1.5rem;
  }
</style>
